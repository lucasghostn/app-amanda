export function validateBackup(data){if(!data||typeof data!=='object'||!Array.isArray(data.transactions)||!Array.isArray(data.categories))throw new Error('Este arquivo não é um backup válido do OrganizaFinanças.');for(const x of data.transactions){if(!['income','expense'].includes(x.type)||!Number.isInteger(x.amount)||x.amount<1||!/^\d{4}-\d{2}-\d{2}$/.test(x.date))throw new Error('O backup contém lançamentos inválidos.')}return data}
